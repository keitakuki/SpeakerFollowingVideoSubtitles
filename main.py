import cv2

def main(video_file_name, audio_file_name, subtitles_file_name):
    video = cv2.VideoCapture(video_file_name)
    audio = 
    subtitles = subtitle_parser()

    speaker = speaker_detection()
    subtitle_placement(speaker, )


def speaker_detection(video_file_path, audio_file_path, subtitles_file_path):

def subtitle_placement():

